/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [], // no external domains needed now
    formats: ['image/webp', 'image/avif'],
  },
}

export default nextConfig
